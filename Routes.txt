Define Routes:
Route::get
Route::post


nullable|boolean
nullable|required_if:insurance,1|String| etc

@if($errors->has('address'))
{{ $errors->first('address') }}
@endif


old('value')
old('value', $docotor->email)

old('value') == 1 ? "checked" : ""; to where it needs to be
old('value') == $patient->id ?